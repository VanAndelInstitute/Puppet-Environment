class mac {
  if($::operatingsystem == "darwin"){
		file { '/etc/test.txt' :
			ensure => present,
			mode 	=> '0600',
			content	=> 'I am only on mac computers',
		}
        
        package { 'pigz':
          ensure    => installed,
          provider  => 'homebrew',
        }

        package { 'google-chrome':
          ensure    => installed,
          provider  => 'homebrew',
        }
  }
}
